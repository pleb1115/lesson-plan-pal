
The user wants direct YouTube video links (not search pages) that are relevant to each module. Currently the code links to youtube.com/results which gets blocked when embedded and is generic.

Need to inspect current implementation to plan properly.
